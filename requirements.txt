# BuildLLM runtime and development dependencies
#
# PyTorch is intentionally not pinned here. Install the CUDA build separately
# so pip does not replace the working GPU enabled installation.
# Current machine command:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# Core data and HTTP dependencies
aiohappyeyeballs==2.7.1
aiohttp==3.14.3
aiosignal==1.4.0
anyio==4.14.2
attrs==26.1.0
certifi==2026.7.22
charset-normalizer==3.4.9
click==8.4.2
colorama==0.4.6
datasets==5.0.0
dill==0.4.1
filelock==3.32.0
frozenlist==1.8.0
fsspec==2026.4.0
h11==0.16.0
hf-xet==1.5.2
httpcore==1.0.9
httpx==0.28.1
huggingface_hub==1.24.0
idna==3.18
Jinja2==3.1.6
MarkupSafe==3.0.3
mpmath==1.3.0
multidict==6.7.1
multiprocess==0.70.19
networkx==3.6.1
numpy==2.4.4
packaging==26.2
pandas==3.0.5
propcache==0.5.2
pyarrow==25.0.0
python-dateutil==2.9.0.post0
PyYAML==6.0.3
requests==2.34.2
six==1.17.0
sympy==1.14.0
tqdm==4.69.0
typing_extensions==4.16.0
tzdata==2026.3
urllib3==2.7.0
xxhash==3.8.1
yarl==1.24.5

# Tokenization and testing
tokenizers>=0.22,<1.0
pytest==9.1.1

# Optional accelerated MediaWiki parser.
# Python 3.14 on Windows currently attempts a local C build that requires
# Microsoft C++ Build Tools. The project already contains a pure Python regex
# fallback, so skip this package on Python 3.14 and newer.
mwparserfromhell>=0.7,<1.0; python_version < "3.14"
